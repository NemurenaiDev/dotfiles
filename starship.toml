"$schema" = 'https://starship.rs/config-schema.json'

add_newline = false

format = """
$username\
$hostname\
$shlvl\
$singularity\
$kubernetes\
$directory\
$vcsh\
$fossil_branch\
$git_branch\
$git_commit\
$git_state\
$git_metrics\
$git_status\
$hg_branch\
$pijul_channel\
$docker_context\
$env_var\
$custom\
$line_break\
$time\
$status\
$container\
$character"""


[character]
success_symbol = "[](bold cyan)[](bold green)"
error_symbol = "[](bold red)[](bold red)"

[directory]
format = 'in [$path]($style)[$read_only]($read_only_style) '
style = "cyan"
read_only = " "
truncate_to_repo = false

[username]
style_root = 'red bold'
style_user = 'purple bold'
format = '[$user]($style)[@](purple bold)'
show_always = true
disabled = false

[hostname]
format = '[$hostname](purple bold) '
ssh_only = false
disabled = false


[fill]
symbol = " "

[git_branch]
symbol = " "
format = "[$symbol$branch]($style) "
style = "bright-black"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "cyan"

[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bright-black"

[git_metrics]
disabled = true
added_style = 'bold blue'
format = '[+$added]($added_style)/[-$deleted]($deleted_style) '

[time]
disabled = false
style = 'cyan'
format = '[\[$time\]]($style) '
